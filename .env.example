# Data Discovery API Configuration

# API Server Settings
API_HOST=0.0.0.0
API_PORT=8000

# Application Settings
DEBUG_MODE=false
DEPLOYMENT_MODE=api
LOG_LEVEL=INFO

# Resource Limits
MAX_FILE_SIZE=10485760
MAX_PROJECTS=50

# MCP Integration Settings
# Set DEPLOYMENT_MODE=desktop when using with Claude Desktop (stdio transport)
# Note: fastapi_mcp only supports SSE transport, not compatible with Claude Desktop
# DEPLOYMENT_MODE=desktop